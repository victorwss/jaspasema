rootProject.name = 'Jaspasema'

def subDirs = rootDir.listFiles(new FileFilter() {
    public boolean accept(File file) {
        return file.isDirectory() && ['core', 'app'].contains(file.name)
    }
});

subDirs.each { File dir ->
    include dir.name
}